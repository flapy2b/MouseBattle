// Import requirejs to import config.json
var requirejs = require('requirejs');

// Import config.json here
var config = require('./config.json');

// Verify the variables
console.log(config.parm1);
console.log(config.parm2);
console.log(config.parm3);
